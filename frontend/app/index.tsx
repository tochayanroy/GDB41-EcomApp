import React from 'react'
import { StyleSheet, View } from 'react-native'
import Home from './CartScreen'

const index = () => {
  return (
    <View style={{flex:1}}>
      <Home/>
    </View>
  )
}

export default index

const styles = StyleSheet.create({})